```bash
# Attach to the environment by:

# Enter the container with bash
docker exec -it anaconda-dev /bin/bash

# List the running sessions
jupyter server list

# Copy the token provided after:
?token=
# to attach the interpreter in VsCode to:

# Or load Jupyter lab in the browser.
```
